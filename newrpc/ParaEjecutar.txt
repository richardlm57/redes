Para compilar:

	make -f Makefile.disp

Activar el demonio portmap en background

	sudo rpcbind &

Ejecutar el servido en background

	sudo ./disp_server &

Si al hacer echo $?, imprime 0, el procedimiento
ha sido corredo.

Ejecutar el cliente:

sudo ./disp_client <dir_ip_servidor> fila
